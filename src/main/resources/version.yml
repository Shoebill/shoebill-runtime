name: Shoebill Runtime
version: ${system.MAVEN_VERSION}
support: SA-MP 0.3.8
buildNumber: ${env.BUILD_NUMBER}
buildDate: ${env.BUILD_START_DATE}
targetApi: ${dep.Shoebill_Api_Deploy.maven.project.version}